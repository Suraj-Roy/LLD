Problem Statement (Simple Version)

Build a cross-platform UI toolkit that can create a family of UI components: Button and Checkbox.

There are two platforms: Windows and Mac.

Each platform has its own style of Button and Checkbox.

The client code should be able to create Buttons and Checkboxes without knowing the concrete classes.

Adding a new platform or new UI component should not require modifying existing client code.